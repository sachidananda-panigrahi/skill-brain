# Server port (default: 3000)
PORT=3000

# Optional: Enable OpenAI embeddings for semantic search
# Without this, skill search uses TF-IDF cosine similarity (zero-config)
# OPENAI_API_KEY=sk-...

# Optional: Override the embedding model (default: text-embedding-3-small)
# EMBEDDING_MODEL=text-embedding-3-small

